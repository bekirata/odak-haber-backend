# ODAK Haber Backend - Railway-compatible requirements
# Last updated: Feb 2026

# Web framework
fastapi>=0.110,<0.116
uvicorn[standard]>=0.25,<0.32
python-multipart>=0.0.20

# Configuration & utils
python-dotenv>=1.0.0
pydantic>=2.5,<3.0

# MongoDB async client
motor>=3.3,<4.0
pymongo>=4.5,<5.0
dnspython>=2.4

# RSS & HTTP (httpx version range required by firebase-admin)
feedparser>=6.0
httpx>=0.28,<1.0
requests>=2.31
python-dateutil>=2.8
pytz>=2024.1

# LLM (Emergent LLM proxy compatible — uses standard OpenAI API)
litellm>=1.40
openai>=1.40

# Firebase Cloud Messaging (FCM v1) — native push notifications
firebase-admin>=7.0

# Image (used by Pillow-based image static endpoints)
pillow>=10.0
